# BoltPage Build Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real credentials!

# =============================================================================
# APPLE CODE SIGNING & NOTARIZATION (macOS builds only)
# =============================================================================
# Required for macOS app signing and notarization
# Get these from your Apple Developer account: https://developer.apple.com

# Your Apple ID email address
APPLE_ID=your-apple-id@example.com

# App-specific password for notarization
# Generate at: https://appleid.apple.com/account/manage (Sign-In and Security > App-Specific Passwords)
APPLE_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Your Apple Developer Team ID (10 alphanumeric characters)
# Find in Apple Developer account or in your certificate
APPLE_TEAM_ID=XXXXXXXXXX

# Your signing identity (optional - usually auto-detected)
# Format: "Developer ID Application: Your Name (TEAM_ID)"
# APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (XXXXXXXXXX)"

# =============================================================================
# WINDOWS CODE SIGNING (Windows builds only)
# =============================================================================
# Optional: Only needed if you have a Windows code signing certificate

# Path to your Windows code signing certificate (.pfx or .p12)
# TAURI_SIGNING_PRIVATE_KEY=path/to/certificate.pfx

# Password for your Windows certificate
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD=your_certificate_password

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. The .env file is gitignored and will never be committed
# 4. For macOS builds, you MUST set APPLE_ID, APPLE_PASSWORD, and APPLE_TEAM_ID
# 5. For Windows builds, code signing is optional but recommended
#
# The build script (boltpage/build-release.sh) will automatically load
# credentials from .env, .env.local, or .env.release files

# =============================================================================
# SECURITY REMINDERS
# =============================================================================
# - NEVER commit .env files to version control
# - Use app-specific passwords for Apple ID (if 2FA is enabled)
# - Keep certificates secure and rotate them regularly
# - Store backups of certificates in a secure location
# - For CI/CD, use GitHub Secrets instead of .env files
